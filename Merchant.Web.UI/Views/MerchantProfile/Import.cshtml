@model MerchantProfileImportViewModel

@{
    ViewBag.Title = "ورود اطلاعات پایانه ها";
}

<div class="page-header">
    <a class="btn btn-info pull-left" target="_blank" href="~/Download/MerchantProfiles.xlsx">نمونه فایل جهت ورود اطلاعات</a>
    <h1>ورود اطلاعات پایانه ها</h1>
</div>

<form enctype="multipart/form-data" action="@Url.Action("Import", "MerchantProfile")" method="post" id="MerchantProfileImportForm">
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <div class="fileinput fileinput-new input-group" data-provides="fileinput">
                    <span class="input-group-addon btn btn-default btn-file"><span class="fileinput-new">انتخاب فایل (.xlsx)</span><span class="fileinput-exists">تغییر</span><input accept=".xlsx" type="file" name="File" id="File"></span>
                    <div class="form-control" data-trigger="fileinput">
                        <i class="glyphicon glyphicon-file fileinput-exists"></i> <span class="fileinput-filename"></span>
                    </div>
                    <span class="input-group-btn">
                        <button id="ConfirmButton" type="button" class="btn btn-success" data-loading-text="در حال ثبت...">ثبت اطلاعات</button>
                    </span>
                </div>
            </div>
        </div>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">وضعیت ها:</b>
        <ul style="display: inline-block" class="list-inline">
            @foreach (var item in Model.TerminalStatusList)
            {
                <li style="border-bottom: 2px solid #FFAB91; padding-bottom: 3px; margin-bottom: 4px;">@item</li>
            }
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">وضعیت های سند مسدودی:</b>
        <ul style="display: inline-block" class="list-inline">
            @foreach (var item in Model.BlockDocumentStatusList)
            {
                <li style="border-bottom: 2px solid #FFAB91; padding-bottom: 3px; margin-bottom: 4px;">@item</li>
            }
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">ملیت ها:</b>
        <ul style="display: inline-block" class="list-inline">
            @foreach (var item in Model.NationalityList)
            {
                <li style="border-bottom: 2px solid #80CBC4; padding-bottom: 3px; margin-bottom: 4px;">@item</li>
            }
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">بازاریاب ها:</b>
        <ul style="display: inline-block" class="list-inline">
            @foreach (var item in Model.MarketerList)
            {
                <li style="border-bottom: 2px solid #FFCC80; padding-bottom: 3px; margin-bottom: 4px;">@item</li>
            }
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">انواع دستگاه:</b>
        <ul style="display: inline-block" class="list-inline">
            @foreach (var item in Model.DeviceTypeList)
            {
                <li style="border-bottom: 2px solid #D1C4E9; padding-bottom: 3px; margin-bottom: 4px;">@item</li>
            }
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">انواع فعالیت:</b>
        <ul style="display: inline-block" class="list-inline">
            @foreach (var item in Model.ActivityTypeList)
            {
                <li style="border-bottom: 2px solid #D7CCC8; padding-bottom: 3px; margin-bottom: 4px;">@item</li>
            }
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">شرکت های PSP:</b>
        <ul style="display: inline-block" class="list-inline">
            @foreach (var item in Model.PspList)
            {
                <li style="border-bottom: 2px solid #F0F4C3; padding-bottom: 3px; margin-bottom: 4px;">@item</li>
            }
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">جنسیت ها:</b>
        <ul style="display: inline-block" class="list-inline">
            <li style="border-bottom: 2px solid #F8BBD0; padding-bottom: 3px; margin-bottom: 4px;">مرد</li>
            <li style="border-bottom: 2px solid #F8BBD0; padding-bottom: 3px; margin-bottom: 4px;">زن</li>
        </ul>
    </div>

    <div class="form-group">
        <b style="margin-left: 10px;">شخصیت ها:</b>
        <ul style="display: inline-block" class="list-inline">
            <li style="border-bottom: 2px solid #D7CCC8; padding-bottom: 3px; margin-bottom: 4px;">حقیقی</li>
            <li style="border-bottom: 2px solid #D7CCC8; padding-bottom: 3px; margin-bottom: 4px;">حقوقی</li>
        </ul>
    </div>

    <div class="table-responsive hidden" id="ErrorsSection">
    </div>
</form>

@section scripts {
    @BundleConfig.AddScripts("~/bundles/merchantProfileImportScripts", "~/Scripts/jasny-bootstrap.js", "~/Scripts/jquery.confirm.js", "~/Scripts/jquery.form.js")

    <script type="text/javascript">
        $(document).ready(function () {
            $('#ConfirmButton').on('click', function (e) {
                if (!$("#File").val()) {
                    toastr.error('فایل مورد نظر را انتخاب نمایید.');
                    return false;
                }

                var $button = $('#ConfirmButton');

                $.showConfirm({
                    onConfirm: function () {
                        var interval = setInterval(function () {
                            toastr.info('اطلاعات در حال ثبت می‌باشد. توجه نمایید که با توجه به حجم اطلاعات ممکن است مدت زمان زیادی برای ثبت صرف شود. لطفاً صبور باشید...');
                        }, 12000);

                        $('#MerchantProfileImportForm').ajaxSubmit({
                             async: true,
                            success: function (result) {
                                clearInterval(interval);
                                $button.button('reset');
                                $.processMessage(result);

                                if (result.success) {
                                    $('#ErrorsSection').addClass('hidden').empty();
                                } else {
                                    clearInterval(interval);
                                    var html = '';
                                    for (var i = 0; i < result.data.length; i++) {
                                        var item = result.data[i];
                                        html += '<div style="margin-top: 15px;" class="alert alert-danger alert-labeled">' +
                                            '<div class="alert-labeled-row"><span class="alert-label alert-labelled-cell">' +
                                            '<i class="fa fa-times"></i></span><p class="alert-labelled-cell alert-body">' + item + '</p>' +
                                            '</div></div>';
                                    }

                                    $('#ErrorsSection').removeClass('hidden').empty().append(html);
                                    $("html, body").animate({ scrollTop: $('#ErrorsSection').offset().top }, 1000);
                                }
                            }, error: function (result) {
                                $button.button('reset');
                                $.processMessage(result);
                                clearInterval(interval);
                            }
                        });

                        $button.button('loading');

                        return false;
                    }
                });
            });
        });
    </script>
}